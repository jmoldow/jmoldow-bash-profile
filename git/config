[user]
	name = Jordan Moldow
	email = jmoldow@alum.mit.edu
[color]
  diff = always
  grep = auto
	status = auto
	branch = auto
  interactive = auto
  ui = auto
  pager = true
[core]
	editor = /usr/bin/vim
	pager = "delta --pager='less -FRXISM'"
	fsmonitor = false
	untrackedcache = false
	commentChar = auto
[interactive]
  diffFilter = "delta --color-only --pager='less -FRXISM'"
[commit]
	cleanup = scissors
[grep]
	lineNumber = true
  extendedRegexp = true
[log]
	decorate = full
[pager]
	status = true
	diff = true
	grep = true
	log = true
	show = true
	branch = true
	describe = true
	notes = true
	range-diff = true
	shortlog = true
	stash = true
	submodule = true
	tag = true
	config = true
	reflog = true
	remote = true
	replace = true
	blame = true
	difftool = true
	help = true
	show-branch = true
	whatchanged = true
[fetch]
  prune = true
[init]
	defaultBranch = main
[push]
  default = simple
[rebase]
  autostash = true
  autosquash = true
[merge]
  tool = vimdiff
  conflictStyle = zdiff3
  branchdesc = true
  autoStash = true
[diff]
  tool = vimdiff
  statGraphWidth = 999
  mnemonicPrefix = true
  renames = copies
  colorMoved = dimmed-zebra
  colorMovedWS = ignore-space-at-eol,ignore-space-change,ignore-all-space
[delta]
  features = line-numbers decorations
  hyperlinks = true
  keep-plus-minus-markers = true
  inspect-raw-lines = true
  paging = always
  line-numbers = true
  true-color = always
  syntax-theme = Dracula
  plus-style = syntax "#003800"
  minus-style = syntax "#3f0001"
  navigate = true    # use n and N to move between diff sections

  # delta detects terminal colors automatically; set one of these to disable auto-detection
  # dark = true
  # light = true
[delta "decorations"]
  commit-decoration-style = bold yellow box ul
  file-style = bold yellow ul
  file-decoration-style = none
  hunk-header-decoration-style = cyan box ul
[delta "line-numbers"]
  line-numbers-left-style = cyan
  line-numbers-right-style = cyan
  line-numbers-minus-style = 124
  line-numbers-plus-style = 28
[alias]
  clean-nffdx   = clean -nffdx
  log-decorate  = log --decorate=full
  log-graph     = log-decorate --oneline --graph
  log-all       = log-graph --all
  log-jordan            = log-decorate --author=jordan --author=Jordan --author=jmoldow --author=Moldow
  log-jordan-graph      = log-jordan --oneline --graph
  log-jordan-all        = log-jordan        --all
  log-jordan-graph-all  = log-jordan-graph  --all
  log-all-jordan    = log-jordan-graph-all
  log-graph-jordan  = log-jordan-graph
  ignore = update-index --assume-unchanged
  amend        = commit --amend --reset-author -e
  commit-amend = commit --amend --reset-author -e
  wip-amend    = commit --amend --reset-author -e --no-verify
  wip          = commit                           --no-verify -m wip
[gc]
  auto = 500
  pruneexpire = 1.weeks.ago
  reflogexpire = 21 days
  reflogexpireunreachable = 21 days
  aggressiveDepth = 20
  aggressiveWindow = 100
[feature]
  manyFiles = false

[diff "scala"]
  xfuncname = "^(\\\\(sub)*section\\{.*)$"

[diff "jmoldowtxt"]
  wordRegex = "[-a-zA-Z0-9']+|[^ a-zA-Z]"
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
