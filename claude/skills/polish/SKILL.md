# Polish Script for Review

1. Review and fix all docstrings and comments in changed files
2. Generate unit tests for any new/modified functions (verify patch targets match actual imports, run tests before presenting)
3. Draft a commit message following conventional commits format
4. Update any affected READMEs
5. Run all tests to confirm nothing is broken

After each step, briefly confirm what was done before proceeding.
